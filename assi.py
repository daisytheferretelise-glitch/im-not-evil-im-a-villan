# Program to find ASCII value of a character

# Take input from user
char = input("Enter a character: ")

# ord() function returns the ASCII value
ascii_value = ord(char)

print(f"The ASCII value of '{char}' is {ascii_value}")
